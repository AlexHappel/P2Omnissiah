<h2>Edit Post</h2>
<form id="edit-post-form">
  <input type="text" name="title" placeholder="Title" value="{{title}}">
  <textarea name="content" placeholder="Content">{{content}}</textarea>
  <button type="submit" data-id="{{id}}">Save Changes</button>
</form>